<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="br.borbi.ots.FailureActivity">

    <TextView android:text="@string/failure_message" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txtFailureMessage"
        android:textSize="@dimen/abc_text_size_large_material"
        android:gravity="center_horizontal" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Try to find me"
        android:id="@+id/btnTryToFindMe"
        android:layout_below="@+id/txtFailureMessage"
        android:layout_centerHorizontal="true"
        android:onClick="tryToFindMeClick" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Continue without my location"
        android:id="@+id/button"
        android:layout_below="@+id/btnTryToFindMe"
        android:layout_centerHorizontal="true"
        android:onClick="continueWithoutMyLocation"
        android:nestedScrollingEnabled="false" />

</RelativeLayout>
